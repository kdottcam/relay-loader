# Relay loader

```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/kdottcam/relay-loader/main/loader.lua"))()
```

Buyers can keep a `key="..."` line above it to skip the key UI. Everyone else gets the Relay key system,
which validates against relayscripts.vercel.app and then hands off to the Sanctuary loader.
